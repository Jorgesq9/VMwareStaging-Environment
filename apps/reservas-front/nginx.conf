server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA típico: si no encuentra el fichero, devuelve index.html
    try_files $uri /index.html;
}
